plugins {
    id('java-platform')
}

group = 'com.github.srcmaxim.platform'

// allow the definition of dependencies to other platforms like the JUnit 5 BOM
javaPlatform.allowDependencies()

dependencies {
    api(platform('org.junit:junit-bom:5.7.1'))
    constraints{
        api('org.assertj:assertj-core:3.19.0')
        api('org.apache.kafka:kafka-streams-test-utils:2.7.0')
//        api('org.hamcrest:hamcrest:2.2')
    }
}
